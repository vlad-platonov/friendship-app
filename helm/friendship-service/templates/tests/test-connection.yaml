apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "friendship-service.fullname" . }}-test-connection"
  labels:
    {{- include "friendship-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "friendship-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
